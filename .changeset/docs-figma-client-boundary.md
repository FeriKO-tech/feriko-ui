---
"@feriko/ui": patch
---

Preserve the React Server Components client boundary in built package output so Next.js apps can import `@feriko/ui` safely from MDX and app-router pages.

Also adds the private Next.js docs app and source-controlled Figma kit artifacts to the monorepo.
